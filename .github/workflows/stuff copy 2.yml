name: After Deployment V3
on:
  deployment_status:
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    if: github.event.deployment_status.state == 'success'
    steps:
    - uses: actions/checkout@v2
    - name: Check project namessssss
      run: echo "${{ github.event.depolyment_status.target_url }}"
    - name: Print all events
      run: echo "${{ toJSON(github.event) }}"
